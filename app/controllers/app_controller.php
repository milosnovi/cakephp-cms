<?php
class AppController extends Controller{
	/**
	 * CakePHP components that are used across all controllers
	 *
	 * @var Array
	 */
	var $components = array('Session', 'RequestHandler', 'Cookie');
}